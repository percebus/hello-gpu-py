[tool.poetry]
name = "hello-gpu-py"
version = "0.1.0"
description = "GPU python excercises"
authors = ["JC Guerrero <JCystems@gMail.com>"]
license = "SEE LICENSE IN LICENSE.md"
readme = "README.md"
packages = [{include = "src"}]

[tool.poetry.dependencies]
python = "^3.12"

# FIXME ~12.6.2 fails
# "Unable to find installation candidates for cuda-python (12.6.2)"
cuda-python = "12.6.1"
cupy-cuda12x = "^13.3.0"
datasets = {extras = ["vision"], version = "^3.1.0"}
logging = "^0.4.9.6"

numpy = "~2.0.0"
pydantic = "^2.9.2"
pydantic-settings = "^2.6.1"
torch = {version = "^2.5.1+cu124", source = "pytorch-gpu"}
torchaudio = {version = "^2.5.1+cu124", source = "pytorch-gpu"}
torchvision = {version = "^0.20.1+cu124", source = "pytorch-gpu"}

[tool.poetry.group.dev.dependencies]
pyhamcrest = "^2.1.0"
pypyr = "^5.9.1"
pytest = "^8.3.3"
pytest-dotenv = "^0.5.2"
ruff = "^0.7.2"


[[tool.poetry.source]]
name = "pytorch-gpu"
url = "https://download.pytorch.org/whl/cu124"
priority = "explicit"

[virtualenvs]
create = true
in-project = true

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
